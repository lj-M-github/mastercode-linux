# Windows/MSYS2 兼容的依赖
# Core LLM and RAG dependencies
langchain>=0.1.0
langchain-community>=0.0.10
langchain-core>=0.1.0
openai>=1.0.0
# chromadb>=0.4.0  # 需要编译，Windows 建议用 Docker 或跳过
sentence-transformers>=2.2.0

# PDF processing
pypdf>=3.0.0
# pdf2image>=1.16.0  # 需要系统安装 poppler，可选
Pillow>=10.0.0

# Ansible execution - Windows 不支持控制节点，跳过
# ansible-core>=2.15.0
# ansible-runner>=2.3.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
typing-extensions>=4.0.0
loguru>=0.7.0
tqdm>=4.65.0

# Development and testing
pytest>=7.0.0
pytest-mock>=3.10.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
